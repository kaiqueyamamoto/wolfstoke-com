<svg width="100" height="100" viewBox="0 0 100 100" fill="none" xmlns="http://www.w3.org/2000/svg">
  <!-- Cabeça do lobo (mais geométrica) -->
  <ellipse cx="50" cy="38" rx="22" ry="26" fill="#000"/>
  
  <!-- Orelhas (triângulos mais definidos) -->
  <polygon points="32,18 36,8 42,18" fill="#000"/>
  <polygon points="58,18 64,8 68,18" fill="#000"/>
  
  <!-- Focinho (mais simples e geométrico) -->
  <ellipse cx="50" cy="44" rx="8" ry="10" fill="#fff"/>
  
  <!-- Nariz (mais pequeno e simples) -->
  <ellipse cx="50" cy="48" rx="3" ry="3.5" fill="#000"/>
  
  <!-- Boca (linha simples) -->
  <path d="M50 50 Q46 52 43 50" stroke="#000" stroke-width="1.5" fill="none" stroke-linecap="round"/>
  
  <!-- Olhos (mais definidos e geométricos) -->
  <circle cx="42" cy="34" r="4.5" fill="#fff"/>
  <circle cx="58" cy="34" r="4.5" fill="#fff"/>
  <circle cx="42" cy="34" r="2" fill="#000"/>
  <circle cx="58" cy="34" r="2" fill="#000"/>
  
  <!-- Corpo (mais simples) -->
  <ellipse cx="50" cy="72" rx="18" ry="22" fill="#000"/>
  
  <!-- Cauda (mais simples e geométrica) -->
  <path d="M68 68 Q80 62 86 68 Q88 72 84 76 Q80 80 70 76 L68 68Z" fill="#000"/>
  
  <!-- Peito (destaque branco no peito - característica de lobo) -->
  <ellipse cx="50" cy="70" rx="10" ry="14" fill="#fff"/>
</svg>
